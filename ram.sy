:A
:/ This is a
   mult:i-line
   co/mment. /:

:: The decleration of all the variables
  int : x;
  intarray[10] : arry;

:: This reads in integers to be added to the array of list
  :F(j:=0;j<10;j := j + 1)
    :I arry[j];
  :.

:: This adds all the integers from the array into a single value
  x := 0;
  :F(i:=0;i<10;i := i + 1)
    x := x + arry[i];
  :.

:O "The sum is: ", x;

:Z
